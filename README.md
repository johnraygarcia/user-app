# user-app

1. Clone the project to your project folder
2. On your terminal/command line, go inside the project folder and run composer install. 
3. Rename the config/config.php.dist to config/config.php and adjust the values of the DB_DSN, DB_USER and DB_PASSWORD constants as  needed.
